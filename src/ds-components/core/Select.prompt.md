Styled native dropdown for filters and short form choices.

```jsx
<Select label="Property type" placeholder="All types"
  options={["Multifamily", "Office", "Retail", "Industrial"]} />
```

`options` accepts strings or `{value,label}`. Sizes `sm`/`md`/`lg`.
