Compact Latent Value Score chip for cards, tables and dense lists.

```jsx
<ScoreBadge score={91} />
<ScoreBadge score={68} showBar />
```
Same heat-scale colours as `ScoreMeter`. Sizes `sm`/`md`. Add `showBar` for a mini meter. Use this inline; use `ScoreMeter` for hero/detail contexts.
