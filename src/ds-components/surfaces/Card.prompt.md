Base surface container with an optional header.

```jsx
<Card title="Price per sqft by type" subtitle="Trailing 90 days" action={<IconButton label="Export"><DownloadIcon/></IconButton>}>
  …chart…
</Card>
```
`padding` none/sm/md/lg · `flat` drops the shadow when nested · `interactive` adds a hover lift.
