# Supabase credentials — copy to .env and fill with your project values.
# Both must be prefixed with VITE_ so Vite exposes them to the client bundle.
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key
